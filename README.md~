# Computer Architecture Labs
This repository contains some of the solutions of the labsheets.

Single Cycle Datapath was attempted.

Main file is SCDDataPath.v, and its testbench is in tb_SCD.v.
For now instruction memory is loaded with R instrutions. Few components are left to support jump and branch instruction.
